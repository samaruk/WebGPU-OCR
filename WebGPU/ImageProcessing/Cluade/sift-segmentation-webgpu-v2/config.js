// config.js — all pipeline constants
export const Config = Object.freeze({
  WG: { X:8, Y:8, FLAT:256, HIST:256 },
  // Preprocess
  CLAHE_TILE:64, CLAHE_BINS:256, CLAHE_CLIP:4.0,
  GAMMA:1.0,
  BILATERAL_SIGMA_S:3.0, BILATERAL_SIGMA_R:0.1, BILATERAL_RADIUS:5,
  GAUSS_SIGMA:1.6, GAUSS_RADIUS:7,
  // Pyramid
  OCTAVES:4, SCALES:5, SIGMA_BASE:1.6,
  K: Math.pow(2, 1/5),
  // SIFT
  CONTRAST_THRESH:0.04, EDGE_THRESH:10.0,
  MAX_KP:16384, DESC_DIM:128,
  ORI_BINS:36, ORI_PEAK_RATIO:0.8, ORI_RADIUS:4.5, ORI_SIG_FAC:1.5,
  DESC_WIDTH:4, DESC_HIST_BINS:8, DESC_MAG_THR:0.2, DESC_SCALE_MUL:3.0,
  SUBPIX_ITERS:5, SUBPIX_THRESH:0.5,
  // Clustering
  GRID_CELL:32, MAX_REGIONS:4096, SIM_THRESH:0.7,
  DENSITY_SIGMA:16.0, LABEL_ITERS:32,
  // Stroke
  RAY_STEPS:64, MAX_STROKE:128.0, MIN_STROKE:1.0, CONSIST_THRESH:0.3,
  // Fusion
  FUSION_ALPHA:0.5, CONF_THRESH:0.4,
  // Segmentation
  BIN_THRESH:0.5, MIN_COMP:64, MAX_LABELS:8192, LABEL_ITERS_SEG:128,
  // Skeleton
  SKEL_ITERS:256,
  // Graph
  MAX_EDGES:65536, MERGE_THRESH:0.6, SPLIT_THRESH:0.4, GRAPH_ITERS:64,
  // Postprocess
  ASPECT_MIN:0.05, ASPECT_MAX:20.0, POLY_EPS:2.0, MIN_AREA:100,
  // Buffer
  UNI_ALIGN:256,
});
export default Config;
